/**
 * Apply alpha to a color value
 * @param {string|number[]} color - Color as hex string or RGB/RGBA array
 * @param {number} [alpha] - Alpha value (0-1)
 * @returns {string} RGBA color string
 */
export default function colorAlpha(color: string | number[], alpha?: number): string;
